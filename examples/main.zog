namespace diamond
# include "setup"

fn &append(&array, &value) {
  &temp = @temp_score(&value)
  data modify storage &{&array} append from storage &{&temp}
}

fn foo() {
  array = [1, 2, 3]
  &append(array, 4)
}
