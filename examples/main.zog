namespace diamond
# include "setup"

fn load() {
  a = [1, 2, 3][1]
  b = [1, 2, 3]
  c = b[2]
  d = 0
  e = b[d]
  index(b, d)
}

fn index(value, %index) {
  return value[%index]
}

fn first(%value) {
  return %value[0]
}
