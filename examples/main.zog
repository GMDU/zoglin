namespace diamond
# include "setup"

fn store_on_temp() {
  &temp = @temp_storage()
  &set_storage(&temp, 10)
  return &temp
}

fn &set_storage(&storage, &value) {
  data modify storage &{&storage} set value &{&value}
}
